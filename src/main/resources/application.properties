# --- Application ---
spring.application.name=javarefresh

# --- Flyway ---
spring.flyway.enabled=true

# --- JPA (common defaults) ---
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=validate

# --- Default profile ---
# Leave commented to avoid forcing one in Git
# spring.profiles.active=dev
